---
// TODO(emily): replace the LinkedIn href with your exact profile URL.
// TODO(emily): drop your resume at public/resume.pdf, then uncomment the
// Resume entry below — kept out until the file exists so nothing 404s.
const links = [
  { label: 'LinkedIn', href: 'https://www.linkedin.com/in/emilydenis' },
  { label: 'Email', href: 'mailto:emilydenis43@gmail.com' },
  // { label: 'Resume', href: '/resume.pdf' },
  { label: 'GitHub', href: 'https://github.com/elabarge3' },
];
---

<footer class="border-t border-zinc-950/5 py-10 dark:border-white/5">
  <div class="mx-auto flex max-w-5xl flex-col items-center justify-between gap-4 px-6 sm:flex-row">
    <p class="text-sm text-zinc-500 dark:text-zinc-400">
      © {new Date().getFullYear()} Emily Denis
    </p>
    <nav class="flex gap-5 text-sm font-medium text-zinc-600 dark:text-zinc-400">
      {links.map((l) => (
        <a href={l.href} class="hover:text-zinc-900 dark:hover:text-white">{l.label}</a>
      ))}
    </nav>
  </div>
</footer>
